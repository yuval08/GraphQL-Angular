enum GraphQLFieldType { String, Integer, Numeric, Time, Object }

enum GQLQueryType { Query, Mutation }

enum GQLSubscriptionErrorType { Connection, Subscription, UnhandledResponseType, GQLError, Stop, Disconnect, SocketError }
